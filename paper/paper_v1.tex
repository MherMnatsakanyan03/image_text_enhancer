\documentclass[sigconf]{acmart}
\usepackage{booktabs}
\usepackage{algorithm}
\usepackage{url}


%% \BibTeX command to typeset BibTeX logo in the docs
\AtBeginDocument{%
  \providecommand\BibTeX{{%
    \normalfont B\kern-0.5em{\scshape i\kern-0.25em b}\kern-0.8em\TeX}}}

%% These commands are for a PROCEEDINGS abstract or paper.
\settopmatter{printacmref=true} % Removes citation information below abstract
%% Was false before, we need to check why its not working as intended
\renewcommand\footnotetextcopyrightpermission[1]{} % removes footnote with conference information in 

\acmConference[AEPRO 2026]{AEPRO 2026: Algorithm Engineering Projects}{March 1}{Jena, Germany}

% convert text to title case
% http://individed.com/code/to-title-case/

% that helps you to formulate your sentences
% https://www.deepl.com/translator

\begin{document}

%%
%% The "title" command has an optional parameter,
%% allowing the author to define a "short title" to be used in page headers.
%% TODO: think about good title
\title{Image Enhancement: A Lightweight, Easy-to-Use, and Efficient C++ Library}
\subtitle{\large Algorithm Engineering 2026 Project}

%%
%% The "author" command and its associated commands are used to define
%% the authors and their affiliations.

\author{Daniel Motz}
\affiliation{%
  \institution{Friedrich Schiller University}
  \city{Jena}
  \country{Germany}}
\email{daniel.motz@uni-jena.de}

\author{Leonard Teschner}
\affiliation{%
  \institution{Friedrich Schiller University}
  \city{Jena}
  \country{Germany}}
\email{leonard.teschner@uni-jena.de}

\author{Mher Mnatsakanyan}
\affiliation{%
  \institution{Friedrich Schiller University}
  \city{Jena}
  \country{Germany}}
\email{mher.mnatsakanyan@uni-jena.de}

%% The abstract is a short summary of the work to be presented in the article.
\begin{abstract}
  TBC
\end{abstract}

%%
%% Keywords. The author(s) should pick words that accurately describe
%% the work being presented. Separate the keywords with commas.
\keywords{task parallelism, WebAssembly, OpenMP, image processing, C++}

%%
%% This command processes the author and affiliation and title
%% information and builds the first part of the formatted document.
\maketitle

\section{Introduction}
\label{sec:intro}
\begin{comment}
  Possible Research Questions
  \begin{enumerate}
    \item Easily extensible Open Source Pipeline, ready to use out of the box with default options giving a perfectly good image, completely written in C++ and licensed under MIT-0 and CeCILL-C, which are both commercially available (compared to e.g. unpaper) and has no big dependencies (unlike unpaper, which uses ffmpeg) and only uses CImg, which is portable (uses OpenMP), simple, self-contained, and lightweight.
    \item Evaluating Performance Gains: How do systematic code optimizations improve the processing speed of a baseline image enhancement tool?
    \item Scaling Performance in Image Processing: Identifying and mitigating inefficiencies in an initial software implementation for OCR 
  \end{enumerate}
\end{comment}

\subsection{Background}
\label{sec:background}

The digitization of documents, especially images scanned or captured with smartphone cameras, is an important step in making information available. It also forms the basis for further processing steps such as optical character recognition (OCR). Existing tools and libraries for image enhancement and document preprocessing are powerful, but often too complex, too specialized, or have many dependencies. This makes it difficult to integrate them into small, user-defined programs and leads to the bloating of these programs. There is therefore a need for a lightweight, easy-to-use, and efficient solution that focuses on image enhancement while being flexible and modular enough to be used in a variety of applications.
As modern smartphone cameras capture images with ever-higher resolutions, the computing time required to process them also increases. Efficient implementation that utilizes the advantages of parallelization and other optimization techniques is therefore important for improving processing speed and ensuring user satisfaction.

\subsection{Related Work}
\label{sec:related-work}

\begin{comment}
Since the introduction of WASM in 2017 \cite{WASM_Haas_2017}, research has progressed in a variety of ways. For instance, significant investment has gone into the 'internal' WASM runtime, either to add features or to investigate its performance and energy efficiency . Another area of research has investigated the use of WASM in various application \cite{wasm_runtimes_survey_zhang_2025} areas, such as web applications, the Internet of Things, and high-performance computing \cite{wasm_runtimes_survey_zhang_2025}. 
Of particular mention is the work of Chadha et al., who examine the use of WASM in the HPC domain in \cite{Mpi_Wasm_Chadha_2023}. Since parallelisation is essential for HPC applications, they have developed a WASM embedder that enables the MPI library to be used in WASM applications.  
\end{comment}
\begin{comment}
  \begin{enumerate}
    \item easy to use
    \item out of the box good results
    \item low dependencies (only CImg (doesnt have other dependencies) and OpenMP )
    \item only image enhancement, no OCR
    \item MIT-0 or CeCILL-C license (commercially usable)
    \item modular (users can choose which steps to apply)
  \end{enumerate}

  Tools die Document Enhancement bereits können:
  - OpenCV -> viel zu groß und abhöngig von vielen anderen Bibliotheken, BSD Lizenz
  - Tesseract - Open Source OCR Engine, die einige Bildverbesserungsfunktionen bietet, aber hauptsächlich auf OCR ausgerichtet ist, nicht einfach erweiterbar, preprocessing sind nicht als allgemeine Enhnancement Methoden gedacht, Apache lizenz
  - Leptonica - C-Bibliothek für Bildverarbeitung, hauptsächlich auf OCR ausgerichtet, BCD Lizenz
  - ImageMagick - Umfassende Kommandozeilen-Tool und Bibliothek für Bildbearbeitung, aber nicht speziell auf die Verbesserung von gescannten oder fotografierten Textbildern ausgerichtet, viele Abhängigkeiten, nicht so einfach erweiterbar, Apache Lizenz
  - Unpaper - spezialisiert auf Nachbearbeitung von Scans von Büchern, viele Abhängigkeiten (ffmpeg etc), nicht so einfach erweiterbar und stark spezialisiert, GPL Lizenz
\end{comment} 

Image enhancement and document preprocessing are covered by a wide range of open-source tools and commercial software. The scope ranges from general purpose image processing libraries and OCR (optical character recognition) libraries to specialized tools for post-processing scanned documents.

\label{sec:general-purpose-libraries}
One of the most widely used libraries for general image processing is OpenCV \cite{opencv}. Described by Bradski in 2000 \cite{opencv_bradski_2000}, OpenCV offers a wide range of functions and algorithms for image processing, such as filters, transformations, segmentation, and feature detection, as well as machine and deep learning. OpenCV is written in C++ and has a modular, cross-platform architecture that allows for easy integration into various applications. However, OpenCV is a very complex library with many functions and dependencies, which leads to overhead when only a fraction of the functions are used. OpenCV is also licensed under the BSD license. 
Similar to OpenCV, ImageMagick \cite{imagemagick} is a comprehensive suite for image processing that is available as a command line tool and as a library. It offers functions such as format conversion, resizing, filtering, and color correction. However, ImageMagick is not specifically designed to enhance scanned or photographed images, but rather for general image processing. The wide range of functions and possibilities results in a large number of dependencies and a large code base. ImageMagick is licensed under the Apache License.

\label{sec:ocr-libraries}
The open source OCR engine Tesseract is often used for document analysis. Tesseract was described by Smith in 2007 \cite{Tesseract_OCR_Smith_2007} and has become one of the leading OCR engines. In addition to text recognition, Tesseract also offers some image enhancement features, but the focus is on OCR. The image enhancement features are typically integrated into the OCR workflow and are not available as general image enhancement features. Tesseract is licensed under the Apache License.
A similar library specializing in OCR is the C-based Leptonica library \cite{leptonica}. Leptonica offers image processing functions that are widely used in the backend of OCR engines such as Tesseract. It provides functions such as binarization, noise reduction, and morphological operations, which are important for preparing images for OCR recognition. Although Leptonica is relatively lightweight compared to the other libraries, it primarily serves as a low-level library and does not offer ready-to-use, configurable processing pipelines, for example. It is also written in C, which makes integration into C++ applications difficult. Leptonica is licensed under the BSD license.

\label{sec:specialized-post-processing}
A more specialized tool is Unpaper \cite{unpaper}, which focuses on post-processing scanned documents. It offers features such as deskewing, binarization, and noise reduction that have been developed specifically for this purpose. However, Unpaper is highly specialized and cannot be flexibly integrated into custom C++ applications. Due to its configuration, Unpaper has many dependencies, which makes it difficult to use. Unpaper is licensed under the GPL license, which restricts commercial use.

\label{sec:intro-optimization}
\begin{comment}
  CoPilots Vorschlag:
  In addition to the tools and libraries described above, there is a large body of research on code optimization and performance improvement in image processing applications. For example, various techniques have been proposed to optimize the performance of image processing algorithms, such as parallelization, vectorization, and algorithmic improvements. However, these optimizations are often implemented in the context of specific libraries or applications and are not always easily transferable to other contexts. Moreover, the performance gains from these optimizations can vary widely depending on the specific use case and implementation details.
\end{comment}

\subsection{Our Contribution}
\label{sec:our-contribution}

Unlike the existing tools and libraries described, our approach is designed to be minimalistic, extensible, and user-friendly. Instead of being a comprehensive image processing library or OCR engine, we focus on a lightweight, modular image enhancement pipeline written entirely in C++ with only one dependency (CImg \cite{cimg}), which is itself lightweight and portable. 
We systematically optimize and improve our intuitive baseline implementation and evaluate the performance gains through various experiments. 
Compared to OpenCV and ImageMagick, our work is characterized by its deliberate focus on image enhancement and compact architecture. Unlike Tesseract and Leptonica, our pipeline is not integrated into an OCR workflow and limited by low-level primitive functions, but offers a configurable standalone solution that is ready for immediate use. Our pipeline is not exclusively specialized in the post-processing of scanned documents, but is designed as a general image enhancement pipeline that can be used for a variety of applications.
In addition, our licensing model (MIT-0 or CeCILL-C) ensures its use in research and commercial applications. By combining minimal dependencies, a modular architecture, portability through C++ and OpenMP, and a commercially usable license, our pipeline fills a gap between large-scale, general-purpose image processing libraries and highly specialized tools for document enhancement.

\subsection{Outline}
\label{sec:outline}

In section \ref{sec:pipeline}, we introduce the pipeline that we will implement and optimize. In section \ref{sec:optimization}, we describe the optimization techniques used. These are evaluated in section \ref{sec:experiments}, where we compare the performance of the pipeline under different settings to the baseline implementation. Finally, we summarize the results in section \ref{sec:conclusions} and provide an outlook on possible future work.

\section{Pipeline}
\label{sec:pipeline}
  
The pipeline is designed to be flexible and modular, enabling users to activate, deactivate and configure each step as required. Each step is designed to run independently of the others, except for the conversion to grayscale step \ref{pl:grayscale}, which forms the basis for subsequent operations. The steps in the pipeline are as follows:

\begin{enumerate}
  \item \label{pl:grayscale} Grayscale Conversion: Conversion of the input image into a grayscale image to simplify processing and reduce computing time.
  \item \label{pl:Deskewing} Deskewing: Correction of skewed images to improve readability and OCR accuracy.
  \item \label{pl:contrast} Contrast Enhancement: Improvement of the contrast in the image to facilitate the distinction between text and background.
  \item \label{pl:denoising} Denoising: Removal of noise and artifacts in the image to improve clarity.
  \item \label{pl:binarization} Binarization: Segmentation of the grayscale image into a binary image consisting of foreground and background.
  \item \label{pl:morphological} Morphological Operations: Application of operations such as erosion and dilation to improve the structure of the text and remove small errors.
  \item \label{pl:color} Color preservation: The color information of the original image is preserved at the locations of the foreground (especially relevant for historical documents to preserve their original coloration).
\end{enumerate}
Several algorithms are implemented in the steps \ref{pl:denoising}, \ref{pl:binarization} and \ref{pl:morphological}. As these have different effects on the result, users can select the algorithm best suited to their application. For instance, Bataineh's adaptive binarisation method with automatic window adjustment \cite{adaptive_thresholding_methods_bataineh_2011} is helpful when binarising historical, handwritten documents with uneven writing and lighting, whereas Otsu's method \cite{Otsu1979} is a good choice for binarising typewritten documents with uniform font and lighting.
Standard configurations are provided for each step, enabling the pipeline to be used straight away. These configurations deliver good results for most use cases and are based on recommendations from the authors. This means that the pipeline can be used immediately, without the need for additional information. However, users can also customise the configurations to optimise the pipeline and improve their specific results.
The pipeline methods \ref{pl:grayscale} - \ref{pl:color} can be used individually by users, as a library in their own program for their specific use case.

We provide a command-line application that can be used independently of a program to access the pipeline. This enables the pipeline to be configured and used directly via the command line. 

\section{Pipeline Optimizations}
\label{sec:optimization}


\begin{comment}
  - Grayscale: parallel for, for pixel processing loop
  - Grayscale: pointer swapping instead of copying the image for each step -> no memory tests were conducted

  - Deskewing: replaced "rotate and project" method with a "radon transform". Instead of moving pixels, we extract foreground coordinates and mathematically project them
  - Deskewing: logic split: split detection and application. Angle is detected once and then applied to the gray and color image
  - Deskewing: total runtime reduced from ~95ms to ~7ms

  - Contrast Enhancement: parallel for unsing thread-local buffers to avoid false sharing/locking
  - Contrast Enhancement: precalculate the linear strecht formula and save in Lookup-Table. main loop performs O(1) lookup insted of floating point calculations
  - Contrast Enhancement: replacing if/else branching in pixel loop with std::min clamping, alowing the compiler to auto-vectorize using SIMD -> no assembly checks were done

  - Adaptive Gaussian Blur: removed std::vector allocations inside hot loop and using "ping-pong" strategie where image acted as destination buffer, swapping pointers at the end
  - Adaptive Median Filter: moving of buffer allocations outside of the pixel loop
  
  - CORE Utilites: returning pre-allocated buffers instead of heavy objects

  - Batainhe Binarization: using integral images to calculate balck and red pixels instead of iterating over the complete window for each pixel -> reduced from windowsize * window size to 4 operations per pixel -> reduzierung von O(N \cdot W^2) zu O(N) 
  - Bataineh Binarization: total runtime reduced from ~340ms to ~20ms
  - Sauvola Binarization: branchless lookup
  - Suavola Binarization: removing of the entire 8-bit image to "CImg<double>" twice. No uses the lightweigt fused vector builder

  - Morphological Operations: decomposing the 2d squre kernel operation intwo two 1d kernel operations (horizontal and vertical) -> reducing the complexity from O(K^2) to O(2K)
  - Morphological Operations: implementation of "sliding Window Min/max" algorithm using a monotonic queue -> calculation for any windowd size in amortized O(1) constant time
\end{comment}

\subsection{Parallelization and Concurrency}
\begin{comment}
  - Grayscale: parallel for, for pixel processing loop
  - Contrast Enhancement: parallel for unsing thread-local buffers to avoid false sharing/locking
  - Deskewing: logic split: split detection and application. Angle is detected once and then applied to the gray and color image
\end{comment}
To achieve better performance on multicore processors, the individual steps are parallelized using OpenMP. \textit{parallel for collapse(3)} environments are mainly used for the pixel processing loops. However, some binarization algorithms also use other parallelization strategies such as the reduction clauses \textit{reduction(min)} and \textit{recuction(max)}. When implementing parallelization, care is taken to ensure that no race conditions or false sharing occur. For example, thread-local buffers are used for contrast enhancement to avoid false sharing and locking. In the deskewing logic, detection and application of the correction are separated. The angle is detected once and then applied to the grayscale and color image to avoid unnecessary calculations. Intuitively, this was done twice.

\subsection{Memory Management and Allocation}
\begin{comment}
  - Grayscale: pointer swapping instead of copying the image for each step -> no memory tests were conducted
  - Adaptive Gaussian Blur: removed std::vector allocations inside hot loop and using "ping-pong" strategie where image acted as destination buffer, swapping pointers at the end
  - Adaptive Median Filter: moving of buffer allocations outside of the pixel loop
  - Suavola Binarization: removing of the entire 8-bit image to "CImg<double>" twice. No uses the lightweigt fused vector builder
  - CORE Utilites: returning pre-allocated buffers instead of heavy objects
\end{comment}
To prevent increased memory consumption and long runtimes, the image is not copied at each step. Instead, the steps work in-place on the input image. For this purpose, a pointer is passed at each step. This approach is continued in grayscale conversion by replacing the final deep copy task of the image with a pointer swap of the original image pointer with the new one. 
Memory optimizations were made in further steps. For example, in the adaptive Gaussian blur filter and the adaptive median filter, the buffer allocations are removed from the pixel processing loop to reduce the number of memory allocations. Sauvola's binarization algorithm uses a lightweight fused vector builder instead of the \textit{8-bit CImg<double>} image structure. Similarly, the core utilities return preallocated buffers instead of heavy objects.

\subsection{Algorithmic Complexity Reduction}
\begin{comment}
  - Batainhe Binarization: using integral images to calculate balck and red pixels instead of iterating over the complete window for each pixel -> reduced from windowsize * window size to 4 operations per pixel -> reduzierung von O(N \cdot W^2) zu O(N) 
  - Morphological Operations: decomposing the 2d squre kernel operation intwo two 1d kernel operations (horizontal and vertical) -> reducing the complexity from O(K^2) to O(2K)
  - Morphological Operations: implementation of "sliding Window Min/max" algorithm using a monotonic queue -> calculation for any windowd size in amortized O(1) constant time
  - Deskewing: replaced "rotate and project" method with a "radon transform". Instead of moving pixels, we extract foreground coordinates and mathematically project them
\end{comment}
Several different algorithms are implemented in the pipeline. Some of these are optimized through algorithmic improvements to reduce complexity. One example is Bataineh binarization, which reduces the number of operations per pixel from $O(W^2)$ to $O(1)$ by using integral images to calculate the number of black and red pixels in the window instead of iterating over the entire window. In morphological operations, the complexity is reduced from $O(K^2)$ to $O(2K)$ by decomposing the 2D kernel operations into two 1D kernel operations for the horizontal and vertical dimensions. In addition, the sliding window min/max algorithm is implemented with a monotonic queue to enable computation for arbitrary window sizes in amortized $O(1)$ time. In the deskewing logic, the “rotate and project” method is replaced by a “Radon transform” method. Instead of moving the pixels, the coordinates of the foreground are extracted and mathematically projected.

\subsection{Loop level Optimizations}
\begin{comment}
  - Contrast Enhancement: precalculate the linear strecht formula and save in Lookup-Table. main loop performs O(1) lookup insted of floating point calculations
  - Contrast Enhancement: replacing if/else branching in pixel loop with std::min clamping, alowing the compiler to auto-vectorize using SIMD -> no assembly checks were done
  - Sauvola Binarization: branchless lookup
\end{comment}
To reduce the runtime of pixel processing loops, logical optimizations are made to the main loops that iterate over the pixels. In contrast enhancement, for example, the linear stretching formula is calculated in advance and stored in a lookup table. As a result, the main loop performs an $O(1)$ query per loop iteration instead of floating-point calculations. In addition, the if/else branch in the pixel processing loop is replaced by std::min clamping. This allows the compiler to auto-vectorize the loop with SIMD. In Sauvola binarization, a branchless lookup is also implemented to improve performance.

\subsection{Architectial Refactoring}
\begin{comment}
  - Deskewing: logic split: split detection and application. Angle is detected once and then applied to the gray and color image
  - CORE Utilites: returning pre-allocated buffers instead of heavy objects
\end{comment}
In some cases, architectural changes were also made to improve performance. For example, in the deskewing logic, detection and application of the correction were separated. The angle is first detected and then applied to the grayscale and color images to avoid unnecessary calculations. In the core utilities, pre-built buffers are returned instead of creating heavy objects. This improves performance.

\section{Experiments}
\label{sec:experiments}

\subsection{Aufbau}
\label{sec:aufbau}
\begin{comment}
  Wir werden die performance (Laufzeit) der Pipeline anhand verschiedener Einstellungen testen. Bspw. unterschiediche Anzahl an Threads, verschieden große Bilder, verschiedene Kombinationen von Methoden.
  OS: WSL on Windows 11 (ist version relevant?)
  Hardware:
    i5 14600kf
    32gb ram
    affinity so wie im benchmark (0-7)
    desktop pc, ohne energiespaarmodus, ohne weitere apps im hintergrund
    auch lüfter an, damit keine temperatur throttles passieren könnten
hab nicht geschaut ob nur ausschließlich p-cores verwendet wurden, aber das wäre meine vermutung
\end{comment}

To evaluate the performance of the pipeline with the optimizations, various experiments are conducted and the results are compared with the times of the intuitive implementation. The experiments examine the runtime when processing images of different sizes with and without parallelization.
The experiments are conducted on a computer with an Intel Core i5-14600kf processor with six performance cores and eight efficiency cores. The computer has 32 GB of RAM. They are run on Windows 11 with the Windows Subsystem for Linux (WSL). Care is taken to ensure that no background applications are running and that power-saving mode is disabled so as not to affect performance. The fans are also activated to prevent temperature throttling. 

Test images are individual images from different OCR datasets. One test image is document text from the FUNSD dataset \cite{FUNSD_Jaume_2019}, which has been scanned and contains a lot of noise. Two additional test images are handwritten documents from the HTR ICFHR 2016 dataset \cite{HTR_Dataset_ICFHR_2016_Toselli}. Both of these images have a resolution of XxY and exhibit bleed-through, whereby text from other pages is visible. For colour documents, a test image from the LRDE Document Binarisation Dataset \cite{Efficient_Multiscale_Sauvola_Binarization_Lazzara_Geraud_2013} is used. This is a scanned excerpt from a French magazine of very good quality containing many small sections of text. It has a resolution of XxY. A test image from the KITA imaging programme Forum is used to demonstrate a combination of historical text, UI elements and colour. All images are provided as JPG files.

To test performance with larger images, the KITA image, which has an initial resolution of XxY, is upscaled to 2k, 4k, 8k and 16k using the ImageUpscaler tool \cite{imageupscaler}. All images are processed using the pipeline and the runtime is measured for each step. Additionally, the images are processed using one and eight threads to evaluate the effects of parallelisation.

\subsection{Results}
\label{sec:results}
Ergebnisse

\section{Conclusions}
\label{sec:conclusions}


%%
%% The next two lines define the bibliography style to be used, and
%% the bibliography file.
\bibliographystyle{ACM-Reference-Format}
\bibliography{literature}


\end{document}
\endinput
%%
%% End of file `sample-sigconf.tex'.